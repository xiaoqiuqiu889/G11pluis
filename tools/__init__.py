"""tools/ — the content-toolchain package for 《革命街没有尽头》.

Sub-packages:
    content-studio/  — FastAPI + SPA for editing scene contracts
    replay-lab/      — Snapshot replay tool (CLI + web)
    batch-simulator/ — Batch simulation driver

Modules:
    four_questions_guard_lib.py  — Core library for the 4-Questions Guard
    four-questions-guard.py       — CLI wrapper for the same
"""
