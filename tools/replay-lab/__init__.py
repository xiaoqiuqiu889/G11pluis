"""replay-lab — snapshot replay tool for 《革命街没有尽头》.

Public surface:
    replay.load_event_log(path)    — parse an event log
    replay.replay(snapshot, events) — the reducer
    cli.main(argv)                 — CLI entrypoint
"""
